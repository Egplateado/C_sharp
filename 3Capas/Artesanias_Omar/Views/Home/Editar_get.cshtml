@using EntityArtesanias
@model Entity_artesanias
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Editar_get</title>
</head>
<body>
    <section>
        <div>
            <form >
                <table>
                    <tr>
                        <td>Nombre</td>
                        <td>@Html.TextBox("Nombre", Model.Nombre)</td>
                        <td>@Html.Hidden("Id", Model.Id)</td>
                    </tr>
                    <tr>
                        <td>Color</td>
                        <td>@Html.TextBox("Color", Model.Color)</td>
                    </tr>
                    <tr>
                        <td>Unidades</td>
                        <td>@Html.TextBox("Unidades", Model.Unidades)</td>
                    </tr>
                    <tr>
                        <td>Precio</td>
                        <td>@Html.TextBox("Precio", Model.Precio)</td>
                    </tr>
                    <tr>
                        <td><input type="submit" value="Editar" formaction="/Home/Editar_post" /></td>
                    </tr>
                </table>
            </form>
        </div>
    </section>
    
</body>
</html>
